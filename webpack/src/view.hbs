<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        :root {
            --color-main: #5fa15f;
            --font-main: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
        }

        body,
        html {
            font-family: var(--font-main);
            width: 100%;
            height: 100%;
            margin: 0;
            background-color: #fafafa;
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            align-items: center;
            margin: auto;
            width: 100%;
            margin-top: 20px;
            max-width: 1000px;
            box-sizing: border-box;
        }

        .card {
            display: flex;
            width: 180px;
            margin: 10px 0;
            flex-wrap: wrap;
            justify-content: center;
            padding: .2em;
            background-color: #f3f3f3;
            box-shadow: 0 2px 5px 2px rgba(0, 0, 0, 0.2);
            min-height: 100px;
        }

        .card__title {
            margin: auto;
            text-align: center;
            width: 100%;
            color: var(--color-main);
            text-decoration: none;
            text-transform: uppercase;
            font: bold 14px var(--font-main);
        }

        .card__link {
            margin: 5% auto;
            padding: 4px 0;
            text-align: center;
            width: 35%;
            border-radius: 2px;
            cursor: pointer;
            text-decoration: none;
            text-transform: uppercase;
            font: bold 11px var(--font-main);
        }
        .card__link:hover {
            text-decoration: underline;
        }
        .card__link--model {
            color: snow;
            background-color: var(--color-main);

        }

        .card__link--template {
            background-color: rgb(0,161,241);
            color: snow;
        }

        .card__wrap {
            width: 100%;
            margin: auto;
            display: flex;
            justify-content: space-between;
        }

        .card__swicth {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            flex-shrink: 0;
        }

        .card__swicth input[type=checkbox] {
            height: 0;
            width: 0;
            visibility: hidden;
        }

        .card__swicth label {
            cursor: pointer;
            text-indent: -9999px;
            width: 50px;
            height: 22px;
            background: grey;
            display: block;
            border-radius: 50px;
            position: relative;
        }

        .card__swicth label:after {
            content: '';
            position: absolute;
            top: 1px;
            left: 1px;
            width: 20px;
            height: 20px;
            background: #fff;
            border-radius: 50px;
            transition: 0.1s;
        }

        .card__swicth input:checked+label {
            background: var(--color-main);
        }

        .card__swicth input:checked+label:after {
            left: calc(100% - 1px);
            transform: translateX(-100%);
        }

        .card__swicth label:active:after {
            width: 50px;
        }
    </style>
</head>

<body>
    <main class="container">
        {{#each files}}
        <div class="card">
            <h2 class="card__title">{{clearText file}}</h2>
            <div class="card__wrap">
                {{#if model}}
                <a class="card__link card__link--model" href="http://localhost:5050/templates/defaults/{{file}}" onclick="window.open(this.href,'emailwindow','height=1000,width=600,left=1000,top=auto,scrollbars=yes,menubar=no'); return false;">
                    Default
                </a>
                {{/if}}
                <a class="card__link card__link--template js--template" href="/?{{file}}" id="{{file}}" onclick="window.open(this.href,'emailwindow','height=1000,width=600,left=1000,top=auto,scrollbars=yes,menubar=no'); return false;">
                    Custom
                </a>
            </div>
            <div class="card__swicth">
                <input class="js-switch" type="checkbox" value="{{file}}-switch" id="{{file}}-switch" />
                <label for="{{file}}-switch"></label>
            </div>
        </div>
        {{/each}}
    </main>
</body>
</html>
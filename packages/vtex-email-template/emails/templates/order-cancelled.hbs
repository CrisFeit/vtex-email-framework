<Header class="partial">{{> Header}}</Header>

<table class="notice">
    <tr class="notice__client">
        <td class="notice__client-wrap">
            <h1 class="heading">
                {{#if clientProfileData.firstName}}
                    {{#compare clientProfileData.firstName '!=' 'isAnonymous'}}
                        Olá, {{clientProfileData.firstName}}.
                    {{/compare}}
                {{/if}}
            </h1>
            <p class="acknowledgment">seu pedido foi cancelado.</p>
        </td>
    </tr>
    <tr class="notice__message">
        <td class="notice__message-wrap">
            <p class="paragraph">
                Oi
                {{#if clientProfileData.firstName}}
                    {{#compare clientProfileData.firstName '!=' 'isAnonymous'}}
                        {{clientProfileData.firstName}}.
                    {{/compare}}
                {{/if}}
                , tudo bem?
                recebemos sua solicitação para cancelarmos o pedido
                <strong>{{orderId}}</strong>
                , já está em processo de cancelamento,
                e o seu pagamento será estornado.
            </p>
        </td>
    </tr>
</table>

<h3 class="subheading">
    Pedido Cancelado #{{orderId}}
</h3>

<Shelf class="partial">{{> Shelf}}</Shelf>
<Totals class="partial">{{> Totals}}</Totals>

<br />
<br />

<table class="disclaimer">
    <tr class="disclaimer__row">
        <td class="disclaimer__wrap">
            <p class="paragraph">
                Queremos muito que você volte a comprar com a gente e
                realce ainda mais sua beleza com os nossos produtos!
            </p>
            <p class="paragraph">
                Até breve!
                <br />
                <strong>
                    Time {{_accountInfo.TradingName}}
                </strong>
            </p>
        </td>
    </tr>
</table>

<Footer class="partial">{{> Footer}}</Footer>